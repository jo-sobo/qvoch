# QVoCh Environment Variables
# Copy this file to .env and adjust values as needed.

# --- Server ---
PORT=17223

# --- Access Control ---
# If set, users must enter this passphrase before accessing the app.
# Leave empty to disable site-wide authentication.
SITE_PASSPHRASE=

# --- WebRTC ---
# Public IP or hostname for NAT traversal. Hostnames are resolved automatically.
# Required when running behind NAT (e.g. on a VPS). Leave empty for local dev.
PUBLIC_IP=
# Optional periodic re-resolve for dynamic DNS/public IP changes.
# 0 disables monitor. Supports "60s" style durations or plain seconds like "60".
PUBLIC_IP_RECHECK_INTERVAL=0
# If true, rebuilds active peer connections when PUBLIC_IP/UDP range changes.
PUBLIC_IP_RECHECK_REBUILD_PEERS=true
UDP_MIN=40000
UDP_MAX=40100

# --- CORS ---
# Comma-separated list of allowed origins for WebSocket connections.
# Leave empty for same-origin only (recommended).
ALLOWED_ORIGINS=

# --- Proxy ---
# Set to "true" if running behind a reverse proxy to trust X-Forwarded-For headers.
TRUST_PROXY=false

# --- Limits ---
MAX_USERS_PER_ROOM=25
MAX_ROOMS=100
CHAT_HISTORY_SIZE=200

# --- Frontend (runtime) ---
# Giphy API key for GIF search. Leave empty to disable.
GIPHY_API_KEY=
# Optional build label shown in UI footer.
# Default Docker build flow sets a non-official label automatically.
# QVOCH_BUILD_ID=
